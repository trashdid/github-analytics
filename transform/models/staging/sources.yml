version: 2

sources:
  - name: github_raw
    database: "{{ env_var('GOOGLE_CLOUD_PROJECT') }}"
    schema: github_raw_data
    tables:
      - name: raw_commits
      - name: raw_issues
      - name: raw_pulls